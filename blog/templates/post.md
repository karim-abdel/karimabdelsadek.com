---
title: {{title}}
date: {{date}}
description: "A short summary for the blog index."
draft: true
---

Start writing here. The title and date appear automatically above the post.

## A section

Use **bold**, *italics*, [links](https://example.com), and ordinary paragraphs.

> A quotation or a short aside.

```python
def example():
    return "Code keeps its formatting."
```

For images, put a file in `images/blog/` and use a relative path from the
published post, for example `![A descriptive caption](../../images/blog/figure.png)`.

Replace this example content, edit the description, and set `draft: false` when
you are ready to publish. Then run `npm run build`.
